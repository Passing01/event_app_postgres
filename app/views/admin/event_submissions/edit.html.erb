<h1>Admin::EventSubmissions#edit</h1>
<p>Find me in app/views/admin/event_submissions/edit.html.erb</p>
<h1>Valider l'événement</h1>
<%= form_with(model: [:admin, @event], url: admin_event_submission_path(@event), local: true) do |form| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>
      <ul>
        <% @event.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :validated %>
    <%= form.check_box :validated %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Retour', admin_event_submissions_path %>
